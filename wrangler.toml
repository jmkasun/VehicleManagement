name = "vehicle-management"
compatibility_date = "2024-03-25"
compatibility_flags = ["nodejs_compat"]
pages_build_output_dir = "dist"

[vars]
NODE_ENV = "production"
# IMPORTANT: Ensure these match your actual DB credentials
# or set them in the Cloudflare Dashboard (Settings > Variables)
MYSQL_PORT = "3306"